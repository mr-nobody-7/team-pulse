/**
 * TeamFore icon exports — all from lucide-react.
 * Import from here, not directly from lucide-react,
 * so icons can be swapped without touching every call site.
 */
export {
  /* ── UI ── */
  AlertTriangle as IconWarning,
  Ban as IconDeactivate,
  BarChart2 as IconReports,
  Bell as IconNotifications,
  Calendar as IconMyLeaves,
  CalendarDays as IconCalendar,
  /* ── Actions ── */
  Check as IconCheck,
  ChevronDown as IconChevronDown,
  ChevronLeft as IconChevronLeft,
  ChevronRight as IconChevronRight,
  ChevronsUpDown as IconSort,
  ClipboardCheck as IconApprovals,
  Download as IconDownload,
  ExternalLink as IconExternalLink,
  FileText as IconAuditLogs,
  Info as IconInfo,
  /* ── Navigation ── */
  LayoutGrid as IconDashboard,
  Lock as IconLock,
  LogOut as IconLogOut,
  Menu as IconMenu,
  Monitor as IconDesktopApp,
  MoreVertical as IconMoreVert,
  Pencil as IconEdit,
  Plus as IconPlus,
  PlusCircle as IconApplyLeave,
  Search as IconSearch,
  Send as IconSend,
  Settings as IconSettings,
  Shield as IconShield,
  Trash2 as IconTrash,
  User as IconUser,
  /* ── Status ── */
  UserCheck as IconHoliday,
  UserPlus as IconUsers,
  Users as IconTeams,
  Users as IconUserGroup,
  X as IconClose,
} from "lucide-react";
